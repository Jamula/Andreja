name: Review completion
run-name: Review completion — ${{ github.event_name }}

on:
  pull_request_target:
    types: [opened, edited, reopened, synchronize, ready_for_review, converted_to_draft, labeled, unlabeled]
  pull_request_review:
    types: [submitted, edited, dismissed]
  pull_request_review_thread:
    types: [resolved, unresolved]
  pull_request_review_comment:
    types: [created, edited, deleted]
  issues:
    types: [edited, labeled, unlabeled, closed, reopened]
  merge_group:
    types: [checks_requested]
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      pr_number:
        description: Pull-request number for diagnosis or a canary rerun
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: >-
    review-completion-${{
      (github.event_name == 'issues' &&
      format('issue-{0}', github.event.issue.number)) ||
      inputs.pr_number ||
      github.event.pull_request.number ||
      github.event.merge_group.head_sha ||
      github.sha }}
  cancel-in-progress: true

jobs:
  evaluate:
    name: Required trusted review policy
    if: github.event_name != 'issues'
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    permissions:
      actions: read
      contents: read
      issues: read
      pull-requests: read
    steps:
      - name: Check out the exact trusted workflow revision
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          ref: ${{ github.workflow_sha }}
          persist-credentials: false

      - name: Evaluate two stable full policy snapshots
        uses: actions/github-script@f28e40c7f34bde8b3046d885e986cb6290c5673b # v7
        with:
          script: |
            const runner = require(
              `${process.env.GITHUB_WORKSPACE}/.github/scripts/run-review-completion.js`);
            await runner.run({ github, context, core });

  dispatch-linked-issue:
    name: Request linked PR reevaluation
    if: github.event_name == 'issues'
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    permissions:
      actions: write
      contents: read
      issues: read
      pull-requests: read
    steps:
      - name: Check out the exact trusted workflow revision
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          ref: ${{ github.workflow_sha }}
          persist-credentials: false

      - name: Request one evaluation per linked PR
        uses: actions/github-script@f28e40c7f34bde8b3046d885e986cb6290c5673b # v7
        with:
          script: |
            const runner = require(
              `${process.env.GITHUB_WORKSPACE}/.github/scripts/run-review-completion.js`);
            await runner.dispatchLinkedIssue({ github, context, core });
