# Testing matrix

This matrix records the local evidence expected for shipped Phase 1A behavior.
Never report unavailable live dependencies as passed.

| Area | Evidence | Command | Current local status |
| --- | --- | --- | --- |
| Task domain and application | Validation, explicit proposal, exact diff/payload/digest revalidation, atomic-store dispatch, idempotent retry, complete, export, delete, expiry | `dotnet test Andreja.slnx --no-build` | Automated |
| Assistant, skill, and channel boundary | Deterministic provider remains offline/default; an in-process OpenAI-compatible server proves selected local and HTTPS profiles, exact typed-tool proposal flow, content/usage mapping, endpoint allowlist and redirect rejection, default TLS wiring, timeout/cancellation, bounded retry/body, retryable/nonretryable errors, malformed JSON/tool schema, secret redaction, file rotation/revocation, external budget stop, and content-free usage. Authoritative tenant/app-user/principal identity is preserved; `ISkillHost` and local-only `IChannelHost` share one grant/consent/disclosure evaluator | `dotnet test Andreja.slnx --no-build` | Automated without external model, paid call, connector token, Copilot entitlement, cloud account, or task mutation before confirmation |
| API and authorization | Anonymous and fake-header rejection, real Development cookie sign-in, antiforgery rejection, authoritative validation, typed DTO lifecycle | `dotnet test Andreja.slnx --no-build` | Automated; no header authentication handler exists |
| Local sign-in smoke | Anonymous UI redirects to passkey sign-in with local-only ReturnUrl; explicit Debug+Development cookie sign-in reaches UI/API; Production and Release cannot map the Development helper; API challenge is JSON 401/403 without redirects | `dotnet test Andreja.slnx --no-build`; Release build; follow-redirect live smoke | UI, security headers, unsafe ReturnUrl and production graph automated; real authenticator evidence remains blocked |
| Development origin | Committed HTTPS launch profile and `PublicOrigin` both use `https://localhost:5001`; documented command names the profile and development certificate | `dotnet test Andreja.slnx --no-build` | Automated |
| Same-host TLS proxy | Real Kestrel plain-HTTP tests prove exact trusted proxy restoration of HTTPS/host/client IP, untrusted spoof rejection, wrong host/origin/port rejection, one-hop chain handling, independent recovery buckets, and global abuse cap | `dotnet test Andreja.slnx --no-build` | Automated without trusting any network range |
| Passkey user handle | A generated ES256 assertion exercises the .NET 10 `PasskeyHandler` with the reserved bootstrap Identity ID; exact `userHandle` resolves and a different handle fails. Actual bootstrap/sign-out/discoverable-sign-in endpoints preserve the ID, while tampered, expired, replayed, ID-mismatched, and challenge-mismatched protected tickets fail before persistence | `dotnet test Andreja.slnx --no-build` | Automated; real browser ceremony remains separately blocked |
| Account mutation protection | Production-DI WAF uses the emitted `X-CSRF-TOKEN` for accepted bootstrap/sign-in/recovery/register/revoke posts and rejects missing/wrong tokens; passkey management rejects missing, expired, tampered, wrong-user/audience/stamp recent-auth markers and consumes a valid marker | `dotnet test Andreja.slnx --no-build` | Automated |
| Enhanced account navigation | Static contract proves idempotent delegated submit/click/change handlers plus documented `Blazor.addEventListener("enhancedload", ...)` registration with delayed-start retry and duplicate-subscription guard; server navigation traverses login/bootstrap/recovery/login with enhanced-navigation requests | `dotnet test Andreja.slnx --no-build` | Automated static/server contract; real browser DOM evidence remains blocked |
| Account discoverability and accessibility | Authenticated shell links to account/security; passkey page exposes production sign-out; anonymous pages omit account navigation; dynamic revoke labels include visible credential names | `dotnet test Andreja.slnx --no-build` | Automated markup/source contract; real assistive-browser evidence remains blocked |
| Interactive circuit delegation | Two actual DI scopes resolve the production `AddHttpClient` typed client with different circuit authentication providers and concurrently prove tenant/principal isolation while sharing one pooled stateless handler; Data Protection token is audience-bound, short-lived, single-use, and claim-complete; wrong audience, expiry, replay, tampering, and conflicting/missing claims fail closed | `dotnet test Andreja.slnx --no-build` | Automated against the real TestServer API and antiforgery filter with null `HttpContext` |
| Tenant and principal isolation | Wrong-tenant/wrong-user/wrong-principal/wrong-purpose proposal, list, confirmation, complete, and delete negatives | `dotnet test Andreja.slnx --no-build` | Deterministic checks automated; database-enforced identity/resource references are in the PostgreSQL suite |
| Semantic profile and provenance | Versioned immutable contracts and pinned JSON-LD fixture; typed tenant/app-user/principal scope; source and record digests; user-stated versus inferred/verified state; conflict coexistence; raw-lineage rejection; atomic single-winner correction/retraction and dependent invalidation; safe pinned/absolute predicate IRIs; least exposure; wrong scope/purpose; default sensitive-inference export exclusion; hard delete/tombstone; runtime-serialized content-minimized audit/export proof; version/class/tamper failures; concurrent lifecycle updates | `dotnet test tests\Andreja.UnitTests\Andreja.UnitTests.csproj --filter FullyQualifiedName~SemanticAssertionConformanceTests`; architecture tests | Automated in memory with no persistence, graph provider, model, embedding, inference engine, credential, or network dependency |
| PostgreSQL | Empty migration, atomic/concurrent bootstrap, concurrent distinct/duplicate/over-limit passkey serialization without lost updates, concurrent last-path revocation safety, exactly-once recent-auth nonce consumption, durable passkey, hashed recovery rotation, security-stamp invalidation, task lifecycle, durable atomic proposal confirmation, UTC/microsecond canonical payload round-trip, process restart, crash before/after commit, conflicting key reuse, concurrent confirmation, composite actor/active-resource references, and two-tenant filtering | `dotnet test tests\Andreja.PostgreSqlIntegrationTests\Andreja.PostgreSqlIntegrationTests.csproj` | Compiled in normal validation; runtime required only when `ANDREJA_TEST_POSTGRES` targets a disposable `andreja_test_*` database; unavailable runs are reported blocked, never skipped as passed |
| Application portability | Canonical encrypted tenant export; strict dry-run; atomic clean-database import; AppUser/principal/task/proposal/audit/receipt relationships; idempotent replay; two distinct concurrent exports serialize before the transaction snapshot; loser observes the committed ledger/dirty state; cancellation, timeout, injected failure, and invalid archive cannot leak the dedicated session lock or poison the pool; envelope tamper rejection; credential/passkey/recovery/token tables remain empty; WAF proves no web mutation endpoint | `dotnet test Andreja.slnx --filter FullyQualifiedName~OperationsContractTests`; `dotnet test tests\Andreja.PostgreSqlIntegrationTests\Andreja.PostgreSqlIntegrationTests.csproj --filter FullyQualifiedName~ApplicationPortabilityTests` | Unit/WAF automated; live suite repeatedly races two archives across fresh disposable databases, verifies exactly one commit and exact replay, then exercises cancellation/timeout/failure release before importing successfully; all databases, archives, and keys are destroyed |
| Architecture | Blazor page injects the typed API client and cannot inject modules, adapters, or EF; every assistant/skill/channel execution or invocation boundary carries distinct `TenantId`, `AppUserId`, and `PrincipalId`; semantic assertion ownership uses distinct typed IDs and its conformance area contains no persistence/graph/embedding provider; skill/channel handlers expose no ambient service, database, credential, secret, token, or HTTP client | `dotnet test Andreja.slnx --no-build` | Automated |
| Accessibility and responsive UI | Semantic headings/labels, live status/error regions, keyboard-native controls, reduced motion, mobile breakpoint, and safe interactive initialization failure | `dotnet test Andreja.slnx --no-build`; manual keyboard and 320/768/1280 px viewport review | Initialization failure automated; manual viewport evidence required before launch |
| Browser E2E | Real WebAuthn bootstrap, restart sign-in/out, recovery, then task proposal/confirm/complete/export/delete at mobile/tablet/desktop widths | `node scripts\evidence\browser-e2e.mjs` against the evidence Compose profile | Edge CDP virtual CTAP2 run passed bootstrap, sign-in, lost-device recovery, used-code rejection, deterministic task lifecycle, restart/reconnect, 320/768/1280 layout, and keyboard basics in evidence #44. Human assistive-technology review remains excluded |
| Telemetry privacy | Task/proposal content attributes suppressed while route/status allowlist remains; fixed-content startup canaries prove all three OTLP signals | `dotnet test Andreja.slnx --no-build`; `pwsh -NoProfile -File scripts\evidence\Test-TelemetryEvidence.ps1` after the evidence stack is ready | Automated suppression plus evidence #44 before/after collector deltas after the complete browser flow and restart; the span delta must exceed a non-health threshold, fixed-content logs and metrics must increase, and prohibited-attribute violations must remain zero |
| Hosted .NET gate | Restore; Debug and Release solution build/test; separate Debug and Release PostgreSQL-project compile | `.github\workflows\dotnet-validation.yml` | Hosted on PR, push to `main`, and `merge_group`; artifacts enumerate included, excluded, and unavailable runtime projects |
| Formatting and docs | Compiler analyzers, solution and PostgreSQL-project format verification, contract and docs consistency | See `docs\development.md` | Required before PR readiness |
| Vulnerabilities | Direct and transitive NuGet advisory scan of the service-free solution and separately restored PostgreSQL project | `pwsh -NoProfile -File .github\scripts\invoke-nuget-vulnerability-scan.ps1` | Required locally and hosted before PR readiness |
| C# SAST | Microsoft DevSkim CLI 1.0.90, pinned and executed locally on the hosted runner | `.github\workflows\dotnet-validation.yml` | CodeQL unavailable while private-repository Code Security is disabled; DevSkim fails on findings after the documented loopback-URL and Debug-guard lexical exclusions |
| OCI supply chain | Two no-cache Buildx OCI builds must have identical manifest/config digests; the sole/default `final` target uses the audited offline cache path; pinned Syft emits SPDX 2.3 and CycloneDX; pinned Grype scans dependency SBOM and final archive; pinned Trivy scans Dockerfile/Compose/IaC; evidence/provenance/OCI config/loaded-image platform must agree; High/Critical findings, unavailable tools/databases, hidden/extra/directory/link/path-traversal inventory, manifest-list ambiguity, invalid SBOM, unsigned/untrusted provenance, digest/platform drift, alternate final targets, or unpinned actions/images fail closed | `pwsh -NoProfile -File scripts\supply-chain\Test-SupplyChainPolicy.ps1`; `pwsh -NoProfile -File scripts\supply-chain\Invoke-NegativeTests.ps1`; operator-held signing and offline verification commands in `docs\operations\self-hosting.md`; `.github\workflows\oci-supply-chain.yml` | Hosted on PR, push to `main`, and `merge_group` with read-only/no-secret permissions; hosted provenance is explicitly unsigned/untrusted, reports and private layers are destroyed without upload, and only separately trusted local operator-held signing can authorize startup/update |
| Selective CI classification | Trusted-base, read-only, paginated PR-file metadata; maintainer-label-event sampling; stable unsampled evidence; classification-failure `unavailable` aggregate artifact; single-response fail-closed merge-group compare; exact Markdown changed-line counts including bullet-shaped body lines; stateful base-aware backtick/tilde fence scanning; versioned path policy; inert-metadata allowlist; actions/CODEOWNERS/agent/skill/unknown-GitHub governance negatives; prose-only, executable docs, real PR #103 patch, generated schema, C#/project/package/SDK, dependency-update configuration, migration, PowerShell/workflow, JS/browser, Docker/Compose, supply-chain, mixed, rename/delete, security boundary, metadata-failure, zero-before future-push, and event full-suite fixtures; every tracked path is classified and every rule is reachable; 20-PR replay with policy/classifier digests and sensitivity economics | `node --test .github\ci\change-classifier.test.js`; `node .github\ci\replay-merged-prs.js --limit 20`; `.github\workflows\selective-ci-shadow.yml`; `docs\operations\selective-ci.md` | **Shadow only.** Every PR emits stable classification/aggregate contexts; only a maintainer `labeled` event runs domain jobs, while weekly/manual runs remain full and non-canceling. Automatic push shadow is deferred; existing workflows remain the main safety net. Runs 32924008713/32927691826 are bootstrap topology/full-parity and fixed-overhead evidence only. Existing required workflows and ruleset 21199927 are unchanged. Promotion is blocked pending exactly 5 eligible docs + 5 full labeled PR samples, real merge-group evidence, independent approvals, and guarded de-shadow/ruleset changes |
| Review completion | Non-Actions exact GitHub App identity; immutable durable canonical policy ledger with comment-projection edit/delete/marker/digest tamper repair; complete latest-source carry-forward so exact-epoch reductions never survive head/retarget/base identity changes; pending-first event/durable/live heads; live-API-authoritative PR/review/comment/close/reopen handling; monotonic versioned mapping CAS; exact head/base/diff identity drift detection with full open-PR pagination; protected-base-only push and live-tip verification; completely paginated base mappings with per-number live-head invalidation before CAS/evaluation; dropped-reopen recovery from closed mappings with conflict-then-sweep retry; merge-group constituent revalidation; event/revision/delivery/run/PR/head/base-bound external IDs; downloaded specialist artifact validation; current reviewer authorization; zero unresolved threads; stable full snapshots; delayed H1/close, live-API/CAS failure, concurrent webhook/reconciliation, startup/dropped-delivery/reconciliation/stale-writer/403/429/App-mismatch/retarget/reused-commit/break-glass and #100/#101/#105 no-ready regressions | `node --test .github\scripts\review-gate-app.test.js`; `.github\workflows\review-gate-app-tests.yml`; `.github\scripts\review-gate-ruleset.js`; `docs\runbooks\review-gate-app.md` | **BLOCKED design package only.** No repository workflow holds a publisher credential, no apply path exists, and the ruleset is unchanged. Activation requires separately authorized/provisioned independent App worker identity, least-privilege attestation, canonical ledger/projection/CAS state, negative/startup/revocation/identity-drift canaries, and a real merge-group canary. |

The API integration fixture uses the real cookie scheme through the explicit
Development-only sign-in endpoint for external API evidence. Its circuit regression
resolves the real typed client from the actual `AddHttpClient` registration in two
concurrent simulated circuit scopes. The scopes use distinct principals while the
factory reuses one stateless pooled handler; each sees only its own tasks. The test
also exercises the Data Protection token service, TestServer API authentication
scheme, antiforgery endpoint, and complete task lifecycle with no `HttpContext`.
Common fake-auth headers remain unauthorized and the Development endpoint is absent
in Production. Architecture tests reject fake/test authentication references,
cookie-forwarding handlers, and any pooled delegating handler that depends on
`AuthenticationStateProvider` or `IHttpContextAccessor`.
